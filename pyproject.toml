[project]
name = "nemo-safe-synthesizer"
dynamic = ["version"]
description = "Safe synthesizer"
readme = "README.md"
authors = [{ name = "NVIDIA", email = "nemo@nvidia.com" }]
requires-python = ">=3.11, <3.15"
license = "Apache-2.0"
license-files = ["LICENSE"]

classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Human Machine Interfaces",
    "Topic :: Software Development",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

dependencies = [
    "faker>=20.0",
    "httpx>=0.27.0",
    "huggingface-hub>=1.3.0,<2",
    "pandas>=2.1.3",
    "pydantic[email]>=2.12.5",
    "pydantic-settings>=2.14.2",
    "pyyaml>=6.0.1",
    "jsonschema>=4.22.0",
    "rich>=14.1.0",
    "structlog>=25.4.0",
    "colorama>=0.4.6",
    "tqdm>=4.67.1",
    "setuptools>=80.0.0",
    "typing-extensions>=4.15.0",
]

[dependency-groups]
dev = [
    "ipywidgets",
    "jupyterlab-widgets",
    "pandas-stubs>=2.1.3",
    "prek>=0.3.1",
    "packaging>=25.0.0",
    "typer>=0.15.0",
    "types-click",
    "types-colorama>=0.4.6",
    "textual",
    "tomlkit>=0.13.0",
    # for the proxy tool used to monitor calls to HF during testing
    "proxy-py>=2.4.10",
    { include-group = "docs" },
    { include-group = "test" },
]

docs = [
    "mike",
    "mkdocs-material",
    "mkdocstrings[python]",
    "griffe-pydantic",
    "mkdocs-gen-files",
    "mkdocs-literate-nav",
    "mkdocs-jupyter",
]

test = [
    "pytest>=9.0.3",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pytest-env>=1.1.0",
    "pytest-subtests>=0.13.1",
    "pytest-timeout>=2.3.1",
    "pytest-xdist>=3.5.0",
]

[project.optional-dependencies]
engine = [
    "anyascii",
    "pycountry",
    "flashtext",
    "category-encoders",
    "dython",
    "dateparser",
    "faker",
    "datasets>=4.8.4",
    "huggingface-hub>=1.3.0,<2",
    "json-repair>=0.60.1",
    "outlines>=1.0.0",
    "prv-accountant",
    "python-stdnum",
    "pandas>=2.1.3, <3",
    "plotly",
    "scikit-learn",
    "range_regex>=0.1.0",
    "tenacity==9.1.4",
    "tldextract",
    "tqdm>=4.67.1",
    "wandb==0.28.0",
]

# the following are deps that have Torch as a dep. We keep them separated out from the engine deps
#
# it's a little annoying to have to split them up like this with redundant bits.
# it allows us to have a cpu and a cuda version of torch+deps
# and allow explicit installation of the cpu depset on linux.

# at some point, do per-subpackage dependencies
# >>> BEGIN GENERATED CUDA RUNTIME EXTRAS - DO NOT EDIT <<<
# Source of truth: cuda_deps.toml.
# Regenerate with: uv run --frozen tools/gen_cuda_deps.py cuda_deps.toml --pyproject pyproject.toml
# Manual edits inside this block will be overwritten.
# Generated extras in this block: cpu, cu129, cu130.
cpu = [
    "accelerate>=1.1.0",
    "bitsandbytes>=0.46.1",
    "flashinfer-python==0.6.14; sys_platform == 'linux'",
    "flashinfer-cubin==0.6.14; sys_platform == 'linux'",
    "gliner",
    "kernels>=0.12.1,<0.15",
    "opacus",
    "peft>=0.18.0",
    "sentence-transformers",
    "openai>=2.25.0",
    "trl>=0.23.0",
    "xgrammar>=0.2.0; sys_platform == 'linux'",
    "torch==2.11.0; sys_platform == 'darwin'",
    "torch==2.11.0+cpu; sys_platform == 'linux'",
    "torchaudio==2.11.0; sys_platform == 'darwin'",
    "torchaudio==2.11.0+cpu; sys_platform == 'linux'",
    "torchvision==0.26.0; sys_platform == 'darwin'",
    "torchvision==0.26.0+cpu; sys_platform == 'linux'",
    "torchao==0.17.0",
    "transformers==5.12.0",
    "triton>=2.0.0; sys_platform=='linux'",
    "vllm==0.26.0; sys_platform=='linux'",
]
cu129 = [
    "accelerate>=1.1.0",
    "bitsandbytes>=0.46.1",
    "flashinfer-python==0.6.14; sys_platform == 'linux'",
    "flashinfer-cubin==0.6.14; sys_platform == 'linux'",
    "gliner",
    "kernels>=0.12.1,<0.15",
    "opacus",
    "peft>=0.18.0",
    "sentence-transformers",
    "openai>=2.25.0",
    "trl>=0.23.0",
    "xgrammar>=0.2.0; sys_platform == 'linux'",
    "flashinfer-jit-cache==0.6.14+cu129; sys_platform == 'linux'",
    "nvidia-cublas-cu12; sys_platform == 'linux'",
    "nvidia-ml-py; sys_platform == 'linux'",
    "torch-c-dlpack-ext",
    "triton>=2.0.0; sys_platform == 'linux'",
    "transformers>=5.12,<5.12.1",
    "torch==2.11.0+cu129; sys_platform == 'linux'",
    "torchaudio==2.11.0+cu129; sys_platform == 'linux'",
    "torchvision==0.26.0+cu129; sys_platform == 'linux'",
    "torchao==0.17.0+cu129; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "vllm==0.26.0+cu129; sys_platform == 'linux'",
]
cu130 = [
    "accelerate>=1.1.0",
    "bitsandbytes>=0.46.1",
    "flashinfer-python==0.6.14; sys_platform == 'linux'",
    "flashinfer-cubin==0.6.14; sys_platform == 'linux'",
    "gliner",
    "kernels>=0.12.1,<0.15",
    "opacus",
    "peft>=0.18.0",
    "sentence-transformers",
    "openai>=2.25.0",
    "trl>=0.23.0",
    "xgrammar>=0.2.0; sys_platform == 'linux'",
    "flashinfer-jit-cache==0.6.14+cu130; sys_platform == 'linux'",
    "nvidia-cublas-cu13; sys_platform == 'linux'",
    "nvidia-ml-py; sys_platform == 'linux'",
    "torch-c-dlpack-ext",
    "triton>=2.0.0; sys_platform == 'linux'",
    "transformers>=5.12,<5.12.1",
    "torch==2.11.0+cu130; sys_platform == 'linux'",
    "torchaudio==2.11.0+cu130; sys_platform == 'linux'",
    "torchvision==0.26.0+cu130; sys_platform == 'linux'",
    "torchao==0.17.0+cu130; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "vllm==0.26.0; sys_platform == 'linux'",
]
# <<< END GENERATED CUDA RUNTIME EXTRAS - DO NOT EDIT >>>

[tool.uv]
# Version pinned in .mise.toml but keep here as a fallback constraint
# Allow current 0.9.x line while staying below 0.10.0
required-version = ">=0.9.30, <0.10.0"

cache-keys = [
    { file = "pyproject.toml" },
    { git = { commit = true, tags = true } },
    { file = "uv.lock" },
]
# >>> BEGIN GENERATED CUDA UV CONFLICTS - DO NOT EDIT <<<
# Source of truth: cuda_deps.toml.
# Regenerate with: uv run --frozen tools/gen_cuda_deps.py cuda_deps.toml --pyproject pyproject.toml
# Manual edits inside this block will be overwritten.
# Generated uv section: tool.uv.conflicts.
conflicts = [
    [
        { extra = "cpu" },
        { extra = "cu129" },
        { extra = "cu130" },
    ],
]
# <<< END GENERATED CUDA UV CONFLICTS - DO NOT EDIT >>>

environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
    # we have to add the following to make the right python packages for linux
    # for downstream use in our linux/arm64 docker images.
    # there's subtle differences in the naming convention on the python side
    # even though they are the same architecture, so we use aarch64
    "sys_platform == 'linux' and platform_machine == 'aarch64'",
]

constraint-dependencies = [
    "GitPython>=3.1.50",
    "PyJWT>=2.13.0",
    "Pygments>=2.20.0",
    "aiohttp>=3.14.0",
    "bleach>=6.4.0",
    "cbor2>=5.9.0",
    "cryptography>=48.0.1",
    "grpcio>=1.80.0",
    "idna>=3.15",
    "jsonpath-ng>=1.8.0",
    "langchain-core>=1.2.28",
    "langsmith>=0.7.31",
    "mcp>=1.28.1",
    "mistune>=3.3.3",
    "nbconvert>=7.17.1",
    "pandas<3",
    "pillow>=12.3.0",
    "pymdown-extensions>=10.21.3",
    "soupsieve>=2.8.4",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.31",
    "requests>=2.33.0",
    "starlette>=1.3.1",
    "tornado>=6.5.7",
    "urllib3>=2.7.0",
]

# >>> BEGIN GENERATED CUDA UV SOURCES AND INDEXES - DO NOT EDIT <<<
# Source of truth: cuda_deps.toml.
# Regenerate with: uv run --frozen tools/gen_cuda_deps.py cuda_deps.toml --pyproject pyproject.toml
# Manual edits inside this block will be overwritten.
# Generated uv sections: tool.uv.sources and tool.uv.index.
[tool.uv.sources]
flashinfer-cubin = [
    { index = "flashinfer-cubin", extra = "cpu", marker = "sys_platform == 'linux'" },
    { index = "flashinfer-cubin", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "flashinfer-cubin", extra = "cu130", marker = "sys_platform == 'linux'" },
]
vllm = [
    { index = "vllm-v0-26-0-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
]
torch = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu130", extra = "cu130", marker = "sys_platform == 'linux'" },
]
torchaudio = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu130", extra = "cu130", marker = "sys_platform == 'linux'" },
]
torchvision = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu130", extra = "cu130", marker = "sys_platform == 'linux'" },
]
flashinfer-jit-cache = [
    { index = "flashinfer-jit-cache-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "flashinfer-jit-cache-cu130", extra = "cu130", marker = "sys_platform == 'linux'" },
]
triton = [
    { index = "pytorch-cu129", extra = "cu129", marker = "sys_platform == 'linux'" },
    { index = "pytorch-cu130", extra = "cu130", marker = "sys_platform == 'linux'" },
]
torchao = [
    { index = "pytorch-cu129", extra = "cu129", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
    { index = "pytorch-cu130", extra = "cu130", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
nvidia-cublas-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cuda-cupti-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cuda-nvrtc-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cuda-nvprof-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cuda-runtime-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cuda-nvml-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cudnn-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cusparse-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cusparselt-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-nccl-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-nvjitlink-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-nvshmem-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-nvtx-cu12 = [
    { index = "pytorch-cu129" },
]
nvidia-cublas = [
    { index = "nvidia-pypi-public" },
]
nvidia-cuda-cupti = [
    { index = "nvidia-pypi-public" },
]
nvidia-cuda-nvrtc = [
    { index = "nvidia-pypi-public" },
]
nvidia-cuda-nvprof = [
    { index = "nvidia-pypi-public" },
]
nvidia-cuda-runtime = [
    { index = "nvidia-pypi-public" },
]
nvidia-cuda-nvml = [
    { index = "nvidia-pypi-public" },
]
nvidia-cudnn = [
    { index = "nvidia-pypi-public" },
]
nvidia-cusparse = [
    { index = "nvidia-pypi-public" },
]
nvidia-cusparselt = [
    { index = "nvidia-pypi-public" },
]
nvidia-nccl = [
    { index = "nvidia-pypi-public" },
]
nvidia-nvjitlink = [
    { index = "nvidia-pypi-public" },
]
nvidia-nvshmem = [
    { index = "nvidia-pypi-public" },
]
nvidia-nvtx = [
    { index = "nvidia-pypi-public" },
]

[[tool.uv.index]]
name = "pytorch-cu129"
url = "https://download.pytorch.org/whl/cu129"
explicit = true

[[tool.uv.index]]
name = "flashinfer-jit-cache-cu129"
url = "https://flashinfer.ai/whl/cu129"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[[tool.uv.index]]
name = "flashinfer-jit-cache-cu130"
url = "https://flashinfer.ai/whl/cu130"
explicit = true

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "nvidia-pypi-public"
url = "https://pypi.nvidia.com"
explicit = true

[[tool.uv.index]]
name = "flashinfer-cubin"
url = "https://flashinfer.ai/whl/"
explicit = true

[[tool.uv.index]]
name = "vllm-v0-26-0-cu129"
url = "https://wheels.vllm.ai/0.26.0/cu129"
explicit = true
# <<< END GENERATED CUDA UV SOURCES AND INDEXES - DO NOT EDIT >>>
[build-system]
requires = ["hatchling", "uv-dynamic-versioning"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
# Fallback when no git tag is available (e.g. shallow clone, Dependabot)
fallback-version = "0.0.0"
vcs = "git"
style = "pep440"

[tool.hatch.build.targets.wheel]
packages = ["src/nemo_safe_synthesizer"]
# Include license files in wheel distribution
artifacts = [
    "LICENSE",
    "THIRD_PARTY.md",
]

[project.scripts]
safe-synthesizer = "nemo_safe_synthesizer.cli.cli:cli"

[tool.ty.rules]
# Platform-specific imports (e.g. vLLM on macOS) use ty: ignore[unresolved-import]
# that appear unused when the package is installed. Suppress the noise.
unused-ignore-comment = "ignore"

[tool.ty.environment]
extra-paths = ["typings"]

[tool.ty.src]
# Below is a list of excluded directories from ty typechecks.
exclude = [
    ".uv_cache", # Cache Dir in CI
    "./docs/**/*.ipynb",
    "./src/nemo_safe_synthesizer/pii_replacer/",
]
